export default function WorkspaceLoading() {
    // Return null to prevent showing a loading skeleton on every
    // client-side navigation. The page content renders instantly
    // from the Zustand store, so no loading state is needed.
    return null;
}
